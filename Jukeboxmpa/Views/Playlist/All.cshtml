@model IEnumerable<Jukeboxmpa.Models.Playlist>

<h2>Alle Playlists</h2>

<p>
    <a asp-controller="Song" asp-action="Index" class="btn btn-secondary">Terug naar liedjes</a>
</p>

@if (!Model.Any())
{
    <p>Er zijn nog geen openbare playlists.</p>
}
else
{
    <ul>
        @foreach (var playlist in Model)
        {
            <li>
                <a asp-action="Details" asp-route-id="@playlist.Id">@playlist.Name</a>
                @if (playlist.IsPublic) { <span class="badge bg-success">Openbaar</span> }
                else { <span class="badge bg-secondary">Privé</span> }
            </li>
        }
    </ul>
}
